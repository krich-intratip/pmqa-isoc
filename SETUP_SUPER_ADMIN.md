# วิธีตั้งค่า Super Admin และเข้าถึง User Management

## 🔐 ปัญหา: ไม่เห็นหน้า User Management

หน้า User Management (`/admin/users`) จะแสดงเฉพาะผู้ใช้ที่มี role:
- `super_admin` (ผู้ดูแลระบบสูงสุด)
- `central_admin` (ผู้ดูแลส่วนกลาง)

---

## ✅ วิธีที่ 1: ใช้หน้า Setup Admin (แนะนำ)

### ขั้นตอน:

1. **เข้าสู่ระบบครั้งแรก:**
   - ไปที่ https://your-app.vercel.app/setup-admin
   - หรือ http://localhost:3000/setup-admin (local development)

2. **Login ด้วย Google:**
   - ระบบจะให้ login ด้วย Google account ของคุณ

3. **กรอกข้อมูล Super Admin:**
   - ชื่อ-นามสกุล
   - Email
   - เบอร์โทรศัพท์
   - ตำแหน่ง
   - ฝ่าย/แผนก
   - หน่วยงาน

4. **คลิก "สร้าง Super Admin Account":**
   - ระบบจะสร้าง account พร้อม role `super_admin` อัตโนมัติ

5. **เข้าสู่ระบบใหม่:**
   - ระบบจะ redirect ไปหน้า Dashboard
   - คุณจะเห็นเมนู Admin tools ทั้งหมด

---

## ✅ วิธีที่ 2: ตั้งค่าผ่าน Firebase Console (Manual)

### ขั้นตอน:

1. **เปิด Firebase Console:**
   - ไปที่ https://console.firebase.google.com/
   - เลือกโปรเจค PMQA ISOC

2. **ไปที่ Firestore Database:**
   - คลิก "Firestore Database" ในเมนูด้านซ้าย
   - คลิก "users" collection

3. **หา User Document ของคุณ:**
   - ค้นหาด้วย email ของคุณ
   - หรือดูจาก uid ที่ได้จาก Firebase Authentication

4. **แก้ไข Role Field:**
   - คลิกที่ document ของคุณ
   - แก้ไข field `role` เป็น `super_admin`
   - แก้ไข field `status` เป็น `approved` (ถ้ายังเป็น pending)
   - คลิก "Update"

5. **Logout และ Login ใหม่:**
   - Logout จากระบบ
   - Login เข้าใหม่
   - คุณจะเห็นเมนู Admin tools

---

## ✅ วิธีที่ 3: ใช้ Script (สำหรับ Developer)

### สร้างไฟล์ script:

\`\`\`typescript
// scripts/create-super-admin.ts
import { initializeApp } from 'firebase/app';
import { getFirestore, doc, updateDoc } from 'firebase/firestore';

// Firebase config
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  // ... other config
};

const app = initializeApp(firebaseConfig);
const db = getFirestore(app);

async function createSuperAdmin(uid: string) {
  const userRef = doc(db, 'users', uid);
  await updateDoc(userRef, {
    role: 'super_admin',
    status: 'approved',
    updatedAt: new Date()
  });
  console.log('Super admin created successfully!');
}

// เรียกใช้ด้วย UID ของคุณ
createSuperAdmin('YOUR_USER_UID');
\`\`\`

---

## 📋 ตรวจสอบ Role ปัจจุบัน

### วิธีที่ 1: ดูใน Firebase Console
1. Firebase Console → Firestore Database
2. เปิด collection "users"
3. หา document ที่มี email ของคุณ
4. ดูค่าใน field `role`

### วิธีที่ 2: ดูใน Browser Console
1. เปิดแอปพลิเคชัน
2. กด F12 เปิด Developer Tools
3. ไปที่ Console tab
4. พิมพ์:
\`\`\`javascript
localStorage.getItem('auth-storage')
\`\`\`
5. ดูค่า `role` ใน JSON ที่แสดง

### วิธีที่ 3: ดูในหน้า Profile
1. ไปที่ `/profile`
2. ดู Badge ที่แสดง role ของคุณ

---

## 🔍 ตรวจสอบว่า User Management แสดงหรือไม่

หลังจากตั้งค่า Super Admin แล้ว ให้ตรวจสอบ:

### ใน Dashboard:
✅ ต้องเห็นการ์ด "Cycle Management"
✅ ต้องเห็นการ์ด "User Management"
✅ ต้องเห็นการ์ด "User Approvals"
✅ ต้องเห็นการ์ด "Activity Log"

### การเข้าถึงโดยตรง:
- `/admin/cycles` - จัดการรอบการประเมิน
- `/admin/users` - จัดการผู้ใช้งาน
- `/admin/approvals` - อนุมัติผู้ใช้ใหม่
- `/admin/activity-log` - ดู Activity Logs

---

## 🎭 7 Roles ในระบบ

| Role | ชื่อภาษาไทย | สิทธิ์หลัก |
|------|-------------|-----------|
| `super_admin` | ผู้ดูแลระบบสูงสุด | ทุกอย่าง |
| `central_admin` | ผู้ดูแลส่วนกลาง | จัดการ users, cycles, activity logs |
| `regional_coordinator` | ผู้ประสานงานภาค | ดูข้อมูลภาค, monitor |
| `provincial_staff` | เจ้าหน้าที่จังหวัด | อัปโหลดหลักฐาน, กรอกข้อมูล |
| `data_owner` | เจ้าของข้อมูล | อัปโหลด, แก้ไขข้อมูลของหมวด |
| `reviewer` | ผู้ตรวจสอบ | อนุมัติหลักฐาน |
| `read_only` | ผู้เข้าชม | ดูข้อมูลอย่างเดียว |

---

## ⚠️ ข้อควรระวัง

1. **ห้ามสร้าง Super Admin หลายคนในครั้งเดียว:**
   - ควรมี Super Admin เพียง 1-2 คน
   - Central Admin สามารถมีได้หลายคน

2. **ตรวจสอบก่อนเปลี่ยน Role:**
   - การเปลี่ยน role จะมีผลทันที
   - ควรมี Super Admin สำรองเสมอ

3. **Security Best Practices:**
   - อย่าแชร์ Super Admin account
   - ใช้ Gmail ที่เป็นทางการ
   - เปิด 2-Factor Authentication

---

## 🆘 ยังไม่แก้ไขได้?

หากทำตามขั้นตอนแล้วยังไม่สามารถเข้าถึง User Management ได้:

1. **ตรวจสอบ Role:**
   - ต้องเป็น `super_admin` หรือ `central_admin`
   - ไม่ใช่ role อื่น

2. **ตรวจสอบ Status:**
   - field `status` ต้องเป็น `approved`
   - ไม่ใช่ `pending`, `rejected`, หรือ `disabled`

3. **Clear Browser Cache:**
   - กด Ctrl+Shift+Delete
   - เลือก "Cookies and other site data"
   - เลือก "Cached images and files"
   - คลิก "Clear data"
   - Login ใหม่

4. **ตรวจสอบ Console Errors:**
   - กด F12
   - ดูใน Console tab
   - มี error แดงๆ หรือไม่

5. **ติดต่อ Developer:**
   - แจ้ง uid และ email ของคุณ
   - แจ้งว่าทำขั้นตอนไหนไปแล้วบ้าง

---

## 📞 Support

หากต้องการความช่วยเหลือเพิ่มเติม:
- สร้าง Issue ใน GitHub Repository
- ติดต่อทีมพัฒนา

---

**Last Updated:** 19 มกราคม 2569 (2026-01-19)
**Version:** 1.5.0

# PMQA ISOC - Version 1.5.0 Release Notes

**Release Date:** 19 มกราคม 2569 (2026-01-19)
**Version:** 1.5.0
**Major Update:** Cycle Integration & Assessment Roadmap

---

## 🎯 สรุปการอัปเดตครั้งนี้

เวอร์ชัน 1.5.0 มุ่งเน้นการ integrate Assessment Cycle Management เข้ากับหน้า data entry หลักของระบบ พร้อมเพิ่มหน้า Roadmap แบบ Interactive เพื่อให้ผู้ใช้เข้าใจ workflow การประเมิน PMQA ได้ง่ายขึ้น

---

## 🆕 ฟีเจอร์ใหม่

### 1. Assessment Roadmap - Interactive Flow Visualization

#### เข้าถึงได้ที่: `/roadmap`

**คุณสมบัติ:**
- ✅ แสดง 8 Phases ของกระบวนการประเมิน PMQA
- ✅ แสดง 18 Tools ที่ใช้ในแต่ละ Phase
- ✅ 2 โหมดการแสดงผล:
  - **Flow Chart View** - แสดงเป็น visual flow พร้อมลูกศร
  - **Detailed View** - แสดงรายละเอียดแต่ละ Phase พร้อม Tools
- ✅ คลิกไปยังเครื่องมือแต่ละตัวได้โดยตรง
- ✅ แสดงระยะเวลาและ deliverables ของแต่ละ Phase
- ✅ Responsive design สำหรับทุกขนาดหน้าจอ

**8 Phases:**
1. **Phase 0: Preparation** (4 tools) - เตรียมความพร้อม
2. **Phase 1: Gap Analysis** (4 tools) - วิเคราะห์ช่องว่าง
3. **Phase 2: Data Collection** (6 tools) - จัดเก็บข้อมูล
4. **Phase 3: Context Analysis** (3 tools) - วิเคราะห์บริบท
5. **Phase 4: SAR Writing** (2 tools) - เขียน SAR
6. **Phase 5: Results Analysis** (2 tools) - วิเคราะห์ผลลัพธ์
7. **Phase 6: Validation** (2 tools) - ตรวจสอบและทดสอบ
8. **Phase 7: Interview Prep** (2 tools) - เตรียมสัมภาษณ์

**ประโยชน์:**
- เห็นภาพรวมของกระบวนการประเมิน PMQA ทั้งหมด
- เข้าใจลำดับขั้นตอนและความเชื่อมโยง
- เข้าถึงเครื่องมือที่ต้องการได้รวดเร็ว

---

### 2. Cycle Selector Component

#### แสดงใน: Header และ Dashboard

**คุณสมบัติ:**
- ✅ แสดง dropdown เลือกรอบการประเมิน
- ✅ แสดงสถานะของรอบ (ร่าง, ใช้งาน, เสร็จสิ้น, เก็บถาวร)
- ✅ Auto-select รอบที่ Active อัตโนมัติ
- ✅ จำรอบที่เลือกไว้ใน localStorage (ไม่ต้องเลือกใหม่ทุกครั้ง)
- ✅ Compact mode สำหรับแสดงใน Header
- ✅ โหลดรายการรอบการประเมินจาก Database

**การใช้งาน:**
1. คลิก dropdown ที่ Header (ด้านบน)
2. เลือกรอบการประเมินที่ต้องการ
3. ระบบจะกรองข้อมูลตามรอบที่เลือกอัตโนมัติ

---

### 3. Evidence Page - Cycle Support

#### หน้า: `/phase1/evidence`

**คุณสมบัติ:**
- ✅ กรองหลักฐานตามรอบการประเมินที่เลือก
- ✅ บันทึก `cycleId` เมื่ออัปโหลดหลักฐานใหม่
- ✅ แสดง warning card เมื่อยังไม่ได้เลือกรอบการประเมิน
- ✅ ป้องกันการอัปโหลดหลักฐานโดยไม่มี cycle context
- ✅ แยกหลักฐานระหว่างรอบการประเมินต่างๆ ได้ชัดเจน

**ประโยชน์:**
- ไม่เกิดความสับสนระหว่างหลักฐานของรอบต่างๆ
- สามารถย้อนกลับดูหลักฐานของรอบเก่าได้
- รองรับการประเมินหลายรอบพร้อมกัน

---

### 4. Data Collector - Cycle Support

#### หน้า: `/phase2/data-collector`

**คุณสมบัติ:**
- ✅ กรองข้อมูล KPI ตามรอบการประเมินที่เลือก
- ✅ บันทึก `cycleId` เมื่อบันทึกข้อมูล KPI
- ✅ แสดง warning card เมื่อยังไม่ได้เลือกรอบการประเมิน
- ✅ Validate cycle selection ก่อนบันทึกข้อมูล
- ✅ ล้างข้อมูลเมื่อไม่มี cycle ที่เลือก
- ✅ แยกข้อมูล KPI ระหว่างรอบการประเมินต่างๆ

**ประโยชน์:**
- ข้อมูล KPI แยกตามรอบการประเมินชัดเจน
- เปรียบเทียบข้อมูลระหว่างรอบได้ง่าย
- ป้องกันการ mix data ระหว่างรอบ

---

## 🔄 การเปลี่ยนแปลง

### Cycle Store Enhancement

**ไฟล์:** `src/stores/cycle-store.ts`

**การเปลี่ยนแปลง:**
- ✅ เพิ่ม `selectedCycle` state
- ✅ เพิ่ม `fetchCycles()` function
- ✅ Auto-fetch cycles จาก Firestore
- ✅ Auto-select active cycle เมื่อโหลดครั้งแรก
- ✅ Persist `selectedCycle` ด้วย localStorage
- ✅ Query cycles เรียงตามปี (desc)

---

## 🗃️ โครงสร้าง Database

### Evidence Collection

```typescript
interface Evidence {
  // เพิ่ม field ใหม่
  cycleId: string; // อ้างอิงรอบการประเมิน

  // fields เดิม...
  id: string;
  categoryId: string;
  unitId: string;
  fileName: string;
  fileUrl: string;
  // ...
}
```

### KPI Data Collection

```typescript
interface KPIData {
  // เพิ่ม field ใหม่
  cycleId: string; // อ้างอิงรอบการประเมิน

  // fields เดิม...
  id: string;
  kpiId: string;
  unitId: string;
  period: string; // YYYY-MM
  value: number;
  target: number;
  status: 'draft' | 'submitted' | 'validated';
  // ...
}
```

---

## 📂 ไฟล์ที่เพิ่ม/แก้ไข

### ไฟล์ใหม่:
- `src/app/roadmap/page.tsx` - Assessment Roadmap page
- `VERSION_1.5.0_NOTES.md` - เอกสารนี้

### ไฟล์ที่แก้ไข:
- `src/app/phase1/evidence/page.tsx` - เพิ่ม cycle support
- `src/app/phase2/data-collector/page.tsx` - เพิ่ม cycle support
- `src/stores/cycle-store.ts` - เพิ่ม selectedCycle และ fetchCycles
- `src/components/cycles/CycleSelector.tsx` - ปรับปรุง UI และ functionality
- `src/components/layout/AppHeader.tsx` - เพิ่ม Cycle Selector
- `src/app/dashboard/page.tsx` - เพิ่มลิงก์ไป Roadmap
- `package.json` - อัปเดตเป็น version 1.5.0
- `CHANGELOG.md` - เพิ่ม release notes v1.5.0

---

## 🚀 วิธีการใช้งาน

### 1. ดู Assessment Roadmap

```
1. Login เข้าระบบ
2. ไปที่ Dashboard
3. คลิก "Assessment Roadmap" ใน Phase 0 section
   หรือไปที่ URL: /roadmap โดยตรง
4. สลับระหว่าง Flow Chart และ Detailed View ได้
5. คลิกที่ชื่อ tool เพื่อไปยังหน้านั้นๆ
```

### 2. เลือกรอบการประเมิน

```
1. ดูที่ Header ด้านบน (ข้างชื่อผู้ใช้)
2. จะเห็น dropdown พร้อมไอคอน Calendar
3. คลิกเพื่อเลือกรอบการประเมิน
4. ระบบจะจำรอบที่เลือกไว้
```

### 3. อัปโหลดหลักฐาน (Evidence)

```
1. เลือกรอบการประเมินจาก Header ก่อน
2. ไปที่หน้า Evidence (/phase1/evidence)
3. ถ้ายังไม่ได้เลือกรอบ จะมี warning card แจ้งเตือน
4. อัปโหลดหลักฐาน - จะถูกบันทึกเข้ารอบที่เลือกอัตโนมัติ
5. หลักฐานจะถูกกรองตามรอบการประเมิน
```

### 4. กรอกข้อมูล KPI

```
1. เลือกรอบการประเมินจาก Header ก่อน
2. ไปที่หน้า Data Collector (/phase2/data-collector)
3. ถ้ายังไม่ได้เลือกรอบ จะมี warning card แจ้งเตือน
4. เลือกปีและเดือนที่ต้องการกรอก
5. กรอกค่า KPI - จะถูกบันทึกเข้ารอบที่เลือกอัตโนมัติ
```

---

## 🔒 ความปลอดภัย

- ✅ ทุก query ใช้ `where('cycleId', '==', selectedCycle.id)` เพื่อกรองข้อมูล
- ✅ Validate cycle selection ก่อนบันทึกข้อมูล
- ✅ ป้องกันการ mix data ระหว่างรอบการประเมิน
- ✅ User ไม่สามารถเข้าถึงข้อมูลของรอบอื่นได้ (เว้นแต่จะเลือกรอบนั้น)

---

## ⏭️ อยู่ระหว่างพัฒนา (Coming Soon)

- 🔄 Gap Tracker - Cycle Support
- 🔄 Risk Analyzer - Cycle Support
- 🔄 SAR Writer - Cycle Support
- 🔄 Results Pack - Cycle Support
- 📊 Dashboard แสดงสถิติตามรอบการประเมิน
- 📈 เปรียบเทียบข้อมูลระหว่างรอบการประเมิน
- 📤 Export ข้อมูลตามรอบการประเมิน

---

## 🐛 Known Issues

- Dashboard ยังแสดงข้อมูล hard-coded (0%, -, etc.) แทนข้อมูลจริงจาก Database
- หน้า phase อื่นๆ ยังไม่รองรับ cycleId (จะอัปเดตในเวอร์ชันถัดไป)
- Cycle Selector ใน Header อาจมีปัญหา hydration ใน SSR (แก้ไขด้วย mounted state)

---

## 📊 สถิติการพัฒนา

**Commits ใน v1.5.0:**
- 725bd2b - Add Assessment Roadmap and Cycle Selector
- 81fea4b - Add Assessment Cycle support to Evidence page
- 1095e04 - Add Assessment Cycle support to Data Collector page

**Files Changed:** 8 ไฟล์
**Lines Added:** ~800 บรรทัด
**Features Added:** 4 features หลัก

---

## 📞 Support

หากพบปัญหาหรือมีข้อเสนอแนะ กรุณาติดต่อทีมพัฒนา หรือสร้าง Issue ใน Repository

---

## 🎓 Learning Points

### สำหรับ Developer:

**1. Cycle Context Pattern**
```typescript
// Import cycle store
import { useCycleStore } from '@/stores/cycle-store';

// Get selected cycle
const { selectedCycle, fetchCycles } = useCycleStore();

// Fetch cycles on mount
useEffect(() => {
  fetchCycles();
}, [fetchCycles]);

// Filter by cycleId in queries
const q = query(
  collection(db, 'your_collection'),
  where('unitId', '==', user.unitId),
  where('cycleId', '==', selectedCycle.id)
);

// Save with cycleId
const newData = {
  ...otherFields,
  cycleId: selectedCycle.id,
};
```

**2. Warning UI Pattern**
```typescript
{!selectedCycle && (
  <Card className="mb-6 border-yellow-200 bg-yellow-50">
    <CardContent className="pt-6">
      <div className="flex items-center gap-3">
        <AlertTriangle className="h-5 w-5 text-yellow-600" />
        <div>
          <p className="font-medium text-yellow-800">
            ยังไม่ได้เลือกรอบการประเมิน
          </p>
          <p className="text-sm text-yellow-700">
            กรุณาเลือกรอบการประเมินจาก Header
          </p>
        </div>
      </div>
    </CardContent>
  </Card>
)}
```

**3. Validation Pattern**
```typescript
const handleSave = async () => {
  if (!selectedCycle) {
    toast.error('กรุณาเลือกรอบการประเมินก่อนบันทึกข้อมูล');
    return;
  }

  // Proceed with save...
};
```

---

## 🙏 ขอบคุณ

ขอบคุณทุกท่านที่ใช้งานระบบ PMQA ISOC และให้ข้อเสนอแนะที่เป็นประโยชน์

---

**PMQA ISOC Development Team**
*กองอำนวยการรักษาความมั่นคงภายในราชอาณาจักร*
